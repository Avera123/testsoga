{% extends '::base.html.twig' %}

{% block body -%}
    <h1>itemsfactura list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Idfact</th>
                <th>Code</th>
                <th>Name</th>
                <th>Cantidad</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('itemsfactura_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.idfact }}</td>
                <td>{{ entity.code }}</td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.cantidad }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('itemsfactura_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('itemsfactura_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('itemsfactura_new') }}">
                NUEVO ITEM
            </a>
        </li>
        <li>
            <a href="{{ path('factura_new') }}">
               NUEVA FACTURA
            </a>
        </li>
        <li>
        <a href="{{ path('tercero') }}">
            NUEVO TERCERO
        </a>
    </li>
    </ul>
    {% endblock %}
