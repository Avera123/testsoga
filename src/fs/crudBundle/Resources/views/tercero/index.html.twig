{% extends '::base.html.twig' %}

{% block body -%}
    <h1>tercero list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Identificacion</th>
                <th>Nombre</th>
                <th>Telefono</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tercero_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.identificacion }}</td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.telefono }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tercero_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tercero_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tercero_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
